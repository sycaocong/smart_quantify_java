server:
  port: 8084

spring:
  application:
    name: smart-quantify-market
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      acks: all

  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: "*"